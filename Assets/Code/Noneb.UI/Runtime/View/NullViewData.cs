namespace Noneb.UI.View
{
    /// <summary>
    /// Just something you can pass as a type parameter when your IViewComponent doesn't give a toss what is the input
    /// </summary>
    public record NullViewData;
}