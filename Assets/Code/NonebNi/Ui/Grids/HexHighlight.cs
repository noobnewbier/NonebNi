using UnityEngine;

namespace NonebNi.Ui.Grids
{
    /// <summary>
    /// Basically act as a tag now as we don't have complicated behaviour, we can worry later.
    /// </summary>
    public class HexHighlight : MonoBehaviour { }
}